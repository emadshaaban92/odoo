<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="slide_channel_invite_view_form" model="ir.ui.view">
            <field name="name">slide.channel.invite.view.form</field>
            <field name="model">slide.channel.invite</field>
            <field name="arch" type="xml">
                <form string="Compose Email" class="o_mail_composer_form">
                    <field name="channel_can_publish" invisible="1"/>
                    <field name="channel_published" invisible="1"/>
                    <field name="channel_visibility" invisible="1"/>
                    <field name="signup_allowed" invisible="1"/>
                    <field name="with_enrollment" invisible="1"/>
                    <group col="1">
                        <div>
                            <div attrs="{'invisible': ['|', ('with_enrollment', '=', True), ('signup_allowed', '=', True)]}" role="alert" class="alert alert-info mb-4">
                                Attendees without account will not be able to create one in order to join the course.
                            </div>
                        </div>
                        <group>
                            <field name="channel_id" options="{'no_open': True}"/>
                            <field name="channel_published" attrs="{'readonly': [('channel_can_publish', '=', False)]}" widget="boolean_toggle"/>
                        </group>
                        <div>
                            <div attrs="{'invisible': ['|', ('channel_id', '=', False), ('channel_published', '=', True)]}" role="alert" class="alert alert-warning">
                                Unpublished courses are not accessible to the attendees.
                            </div>
                        </div>
                        <group col="2">
                            <field name="channel_invite_url" readonly="1" widget="CopyClipboardChar"
                                attrs="{'invisible':['|', ('with_enrollment', '=', True), ('channel_visibility', '!=', 'public')]}"
                                class="mb16"/>
                        </group>
                        <group col="2">
                            <field name="partner_ids"
                                widget="many2many_tags_email"
                                placeholder="Add contacts..."
                                required="1"
                                context="{'force_email':True, 'show_email':True, 'no_create_edit': True, 'no_quick_create': True}"/>
                        </group>
                        <group col="2">
                            <field name="render_model" invisible="1"/>
                            <field name="subject" placeholder="Subject..."/>
                        </group>
                        <field name="can_edit_body" invisible="1"/>
                        <field name="body" class="oe-bordered-editor" options="{'style-inline': true}" attrs="{'readonly': [('can_edit_body', '=', False)]}" force_save="1"/>
                        <group>
                            <group>
                                <field name="attachment_ids" widget="many2many_binary"/>
                            </group>
                            <group>
                                <field name="template_id" label="Use template" context="{'default_model': 'slide.channel.partner'}"/>
                            </group>
                        </group>
                    </group>
                    <footer>
                        <button string="Send" name="action_invite" type="object" class="btn-primary" data-hotkey="q"/>
                        <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="z"/>
                    </footer>
                </form>
            </field>
        </record>
    </data>
</odoo>
