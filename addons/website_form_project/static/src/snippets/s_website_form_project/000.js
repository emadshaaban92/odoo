odoo.define('website_form_project.s_website_form_project', function (require) {
    'use strict';

    const websiteForm = require('website.s_website_form');

    // We do not change any of the functionality of s_website_form thus we just return it
    return websiteForm;
});
